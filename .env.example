# Only used when running tracker.py / uvicorn directly (see "manual development setup").
# Docker Compose sets DB_URL itself and does not read this file.
DB_URL=postgresql://admin:secretpassword@localhost:5432/orderbook
